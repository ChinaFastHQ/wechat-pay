# Example server

Copy `.env.example` to `.env`, leave `CHINA_PAY_MODE=mock` for local UI development, and run `pnpm dev`. Live mode needs all WeChat merchant credentials and a public HTTPS webhook URL.
