# Vanilla web example

Run the server in `../server`, then `pnpm dev`. This is a complete framework-free Native QR/H5/JSAPI client; a production JSAPI call must include the current user's server-issued `openId`.
