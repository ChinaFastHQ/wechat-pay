# React web example

Run the server in `../server`, then `pnpm dev`. Desktop renders the returned Native Pay URL as a QR code. For real H5 or JSAPI tests, expose both sites over registered HTTPS domains and provide the authenticated user's `openId` to `pay()` inside WeChat.
